# Rust — artefactos de compilación en cualquier crate del monorepo
**/target/

# Cargo.lock solo en la raíz del repo (cada crate puede tener el suyo local)
/Cargo.lock

# React.js
/node_modules
/build
/dist
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock
